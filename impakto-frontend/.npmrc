# Hoist dependencies to the root node_modules.
# This simplifies tooling configuration (ESLint, TS) by allowing them to find plugins in the root.
hoist=true

# Automatically link workspace packages.
# When we run `pnpm add @impakto/ui` in an app, it will link to the local folder
# instead of trying to download it from the registry.
link-workspace-packages=true

# Enforce strict peer dependency resolution.
# This ensures that we don't accidentally install incompatible versions of React.
auto-install-peers=true
strict-peer-dependencies=false